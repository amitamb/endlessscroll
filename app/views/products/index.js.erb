$('.products').append('<%= j render(@products) %>');
<% if @products.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@products, :renderer => BootstrapPagination::Rails) %>');
<% else %>
  $('.pagination').remove();
<% end %>
$(window).trigger("process_mosaic");